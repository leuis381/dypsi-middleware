{
  "name": "dypsi-middleware",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "validate-menu": "node ./scripts/check_menu.js",
    "lint": "eslint . --ext .js",
    "lint:fix": "eslint . --ext .js --fix"
  },
  "devDependencies": {
    "ajv": "^8.12.0",
    "ajv-formats": "^2.1.1",
    "eslint": "^8.45.0"
  }
}
